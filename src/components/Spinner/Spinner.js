import './Spinner.scss';
import React from 'react';
import PropTypes from 'prop-types';

const Spinner = props => {
    return (
      <div className="loader">
      </div>
    );
};

Spinner.propTypes = {};

export default Spinner;
